name: Project Packages

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup Project
        run: |
          bash -x ./scripts/setup-project.sh

      - name: Clean Up Extra Files
        run: |
          find . -type d -name '.git' | xargs rm -rf

      - name: Make Source Tarball
        run: |
          mkdir iotc-c-generic-sdk-src/
          tar -zcf iotc-c-generic-sdk-src/sources.tar.gz --exclude='./iotc-c-generic-sdk-src' .

      #use wildcards so that directory path is preserved.
      - name: Publish Source Artifacts
        uses: actions/upload-artifact@v2
        with:
          name: iotc-c-generic-sdk
          path: iotc-c-generic-sdk-src
